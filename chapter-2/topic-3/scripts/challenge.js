const numbers = [2, 5, 8, 12, 15, 20, 17, 22, 25];

function findSumOfEven(arr) {
    // Code here, it has to return the sum number (even) of array
}

function findAverageOfOdd(arr) {
    // Code here, it has to return the average number (odd) of array
}

function findMax(arr) {
    // Code here, it has to return the max number of array
}

function findMin(arr) {
    // Code here, it has to return the min number of array
}
